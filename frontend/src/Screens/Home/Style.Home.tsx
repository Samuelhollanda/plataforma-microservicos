import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    title: {
        marginTop: 15,
        padding: 15,
        fontSize: 30,
        fontWeight: 'bold',
        color: '#000000ff',
    }
})

export default style;