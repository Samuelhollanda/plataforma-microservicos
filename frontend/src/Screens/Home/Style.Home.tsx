import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    title: {
        fontSize: 30,
        fontWeight: 'bold',
        color: '#000000ff',
    }
})

export default style;